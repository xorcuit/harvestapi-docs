---
title: 'Search Profile Services'
openapi: 'GET /linkedin/service-search'
---

import GeoIdSnippet from '/snippets/geo-id.mdx';

## Examples

<CodeGroup>

```javascript JavaScript
const params = new URLSearchParams({
  search: 'Web Development',
  location: 'Australia',
  page: '1',
});
fetch(`https://api.harvest-api.com/linkedin/service-search?${params.toString()}`, {
  headers: { 'X-API-Key': '<api-key>' },
})
  .then((response) => response.json())
  .then((data) => console.log(data));
```

```bash cURL
curl --request GET \
  --url https://api.harvest-api.com/linkedin/service-search?search=Web%20Development&location=Australia&page=1 \
  --header 'X-API-Key: <api-key>'
```

```python Python
import requests

url = "https://api.harvest-api.com/linkedin/service-search?search=Web%20Development&location=Australia&page=1"

headers = {"X-API-Key": "<api-key>"}

response = requests.request("GET", url, headers=headers)

print(response.text)
```

</CodeGroup>

<GeoIdSnippet />

---
title: 'Company posts'
openapi: 'GET /linkedin/post-search'
---

import GetCompanyId from '/snippets/get-company-id.mdx';

<CodeGroup>

```javascript JavaScript
const params = new URLSearchParams({
  company: 'https://www.linkedin.com/company/google',
  // companyId: '1441', // Or Google company ID (faster response)
  postedLimit: 'month',
  page: '1',
});
fetch(`https://api.harvest-api.com/linkedin/post-search?${params.toString()}`, {
  headers: { 'X-API-Key': '<api-key>' },
})
  .then((response) => response.json())
  .then((data) => console.log(data));
```

```bash cURL
curl --request GET \
  --url https://api.harvest-api.com/linkedin/post-search?companyId=1441&postedLimit=month&sortBy=date&page=1 \
  --header 'X-API-Key: <api-key>'
```

```python Python
import requests

url = "https://api.harvest-api.com/linkedin/post-search?companyId=1441&postedLimit=month&sortBy=date&page=1"

headers = {"X-API-Key": "<api-key>"}

response = requests.request("GET", url, headers=headers)

print(response.text)
```

</CodeGroup>

<GetCompanyId />
